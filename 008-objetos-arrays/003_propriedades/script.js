let nome = 'João';

console.log(nome.length); //somente  a palavra depois do . é uma propriedade 

let numeros = [1, 2, 3, 4, 5, 6, 7];

console.log(numeros.length);
console.log(numeros[length]);
console.log(numeros[1]);